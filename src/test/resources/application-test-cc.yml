spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none    
    
# datasources
ccdb:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    
ccdb-admin:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:     

management:
  metrics:
    export:
      stackdriver:
        project-id: sdc-cc-test
        enabled: false
